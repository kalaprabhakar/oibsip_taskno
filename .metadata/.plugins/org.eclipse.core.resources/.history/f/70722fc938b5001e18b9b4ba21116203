package com.kuu.service;

import java.util.ArrayList;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.kuu.dao.IReservationDAO;
import com.kuu.model.User;

@Service("servie")
public class ReservationServiceImpl implements IReservationService {
	
	@Autowired
	private IReservationDAO dao ;

	@Override
	public boolean checkLoginDetails(User usser) throws Exception {
		
		List<String> list = new ArrayList<>();
		
		list = dao.userLogin();
		
		User user = new User();
		
		if(user.getUserName().equals(list.get(0)) && user.getPassword().equals(list.get(1))) {
			return true ;
		}
		
		return false;
	}

}
